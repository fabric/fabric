class Connection(object):
    def __init__(self, host, user=None, port=None):
        """
        .. note::
            If both shorthand and kwarg user/port are given, `.Connection`
            `refuses the temptation to guess
            <http://legacy.python.org/dev/peps/pep-0020/>`_ and instead raises
            an exception.
        """
        pass
